import { styled } from 'styled-components';

const CreatePortfolioFilter = () => {
  return <StFilterListContainer></StFilterListContainer>;
};

const StFilterListContainer = styled.div`
  padding: 1rem;
  border: 1px solid;
`;

export default CreatePortfolioFilter;
