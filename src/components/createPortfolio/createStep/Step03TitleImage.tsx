import { CreatePortfolioStepProps } from '@src/types/portfolioType';

const Step03TitleImage = ({ onNextButtonClick }: CreatePortfolioStepProps) => {
  return <>STEP 3</>;
};

export default Step03TitleImage;
